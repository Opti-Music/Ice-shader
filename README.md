# Ice-shader

Create a file named icemelt.frag in your mods/shaders/ folder. This shader uses a noise generation algorithm to shift the screen's pixels downward.

Place the script inside your song folder. It initializes the shader and gives you an easy function to trigger the melt transition whenever you want during the song.
